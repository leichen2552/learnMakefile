问题背景：
大型项目的代码成千上万，完整编译的时间较长
编写模块代码时，可通过编译检查语法错误
为了提高开发效率，需要支持指定模块的独立编译

将模块名（module）作为目标名（伪目标）建立规则
目标（module）对应的依赖为 build build/module
规则中的命令进入对应的木块文件夹进行编译
编译结果存放于build文件夹下

编写模块代码时可以通过模块独立编译快速检查语法错误
自动变量只能在规则的命令中使用，不能在依赖中使用
makefile中自定义函数是代码复用的一种方式
当规则中的命令大量重复时，可考虑自定义函数
